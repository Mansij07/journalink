apiVersion: v1
kind: Service
metadata:
  name: journalink
  namespace: journalink
  labels:
    app: journalink
spec:
  type: ClusterIP
  selector:
    app: journalink
  ports:
    - name: http
      port: 80
      targetPort: 3000
